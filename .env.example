# 选择提供商: openai 或 ollama
PROVIDER=openai
MODEL=gpt-4o-mini

PROVIDER=gemini
MODEL=gemini-1.5-flash  # 或 gemini-1.5-pro

# OpenAI 兼容端点（官方或第三方代理）
OPENAI_API_KEY=sk-xxxxx
GEMINI_API_KEY=API_KEY
# 可选：自定义 Base URL（例如 OpenRouter 或自建代理）
# OPENAI_BASE_URL=https://api.openai.com/v1

# Ollama (本地运行) 参数（通常无需配置，仅需安装 ollama 并拉取模型）
# OLLAMA_HOST=http://localhost:11434
PROVIDER=gemini
MODEL=gemini-1.5-flash  # 或 gemini-1.5-pro

